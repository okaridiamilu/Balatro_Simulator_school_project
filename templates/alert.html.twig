{# Macro Twig pour afficher des alertes Bootstrap (utilisée avec import dans d'autres templates) #}
{% macro alert(type, message) %}
    <div class="alert alert-{{ type }}">
        {{ message }}
    </div>
{% endmacro %}